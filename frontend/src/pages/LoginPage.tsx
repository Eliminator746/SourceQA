import LoginForm from "../components/LoginForm";

export default function LoginPage() {
  return (
    <main>
      <section>
        <h1>RAG Q&A</h1>

        <p>Sign in to access your documents and ask questions.</p>

        <LoginForm />
      </section>
    </main>
  );
}
